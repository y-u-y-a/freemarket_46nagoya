.single-container
  = render "users/signup_header"

  %main.single-main
    %section.l-single-container
      %h2.l-single-head 電話番号の確認
      = form_for(@user, html: {class: 'l-single-inner'}) do |f|
        = render "devise/shared/error_messages", resource: @phone_number

        .l-single-content
          .form-group
            = f.label :携帯電話の番号
            = f.text_field :nickname, placeholder: "携帯電話の番号を入力", class: 'input-default'
            %p.l-single-text
              本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。

        .l-single-content
          %button.btn-default.btn-red{type: "submit"} SNSを送信する
          %p.l-single-text
            電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
          .form-group.text-right
            %p
              %a{href: "", target: "_blank"}
                電話番号の確認が必要な理由
                %i.fa.fa-angle-right

  = render "users/user_footer"

